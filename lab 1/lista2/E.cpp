#include <iostream>

using namespace std;

int main() {
    int pontos;
    cin >> pontos;

    int pares[pontos];
    int impares[pontos];

    for(int i = 1; i <= pontos; i++) {
        if(i % 2 == 0) {
            cin >> pares[i];
        } else {
            cin >> impares[i];
        }
    }

    for(int)

    return 0;
}